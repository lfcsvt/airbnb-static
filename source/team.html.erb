<div class = "text-center"><h1>TEAM</h1></div>
  <ul class="list-inline text-center">
    <% ["kostas", "sjobs", "thomas"].each do |member| %>
      <li>
        <%= image_tag "#{member}.jpeg", class: "img-circle"%>
        <h3><%= member.capitalize %></h3>
      </li>
    <% end %>
  </ul>
